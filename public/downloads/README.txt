Official Android APK (dgc-dbms.apk) is produced by GitHub Actions:
  Actions → "Build Android APK" → Run workflow

Requires repository secret: ANDROID_KEYSTORE_PASSWORD

After a successful run, dgc-dbms.apk is committed here or available as a workflow artifact.
