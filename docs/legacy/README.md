# Legacy artifacts

## Prisma (`prisma-schema.prisma`)

This repo previously included a default Next.js Prisma stub. The live app uses **Firebase Firestore** only. The schema file is archived here so it does not imply Postgres is configured or required.
